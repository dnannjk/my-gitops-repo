apiVersion: apps/v1
kind: Deployment
metadata:
  name: springboot-github-workflow-deployment
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: 767398147229.dkr.ecr.us-east-1.amazonaws.com/gitworkflowrepo:.*
          ports:
            - containerPort: 8080

